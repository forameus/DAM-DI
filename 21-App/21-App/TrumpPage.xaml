<Page
    x:Class="_21_App.TrumpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_21_App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource fondoTrump}">
        <TextBlock Text="¿Casualidad o coincidencia?" Style="{StaticResource txtTrumpTitulo}"/>

        <StackPanel MaxHeight="500" MaxWidth="500">
            <Image Name="fotoTrump" Source="Assets/trump1.jpg" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Style="{StaticResource btnTrumpOk}" Grid.Column="1" Click="Button_Click"/>
                <Button Style="{StaticResource btnTrumpNo}" Grid.Column="0" Click="Button_Click"/>
            </Grid>
        </StackPanel>
        

    </Grid>
</Page>
